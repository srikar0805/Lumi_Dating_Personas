// Production environment.
// Replace `apiUrl` with the deployed backend URL before building for production.
// Example: 'https://lumi-api.onrender.com/api'
export const environment = {
  production: true,
  apiUrl: 'https://YOUR-DEPLOYED-API.example.com/api',
};
